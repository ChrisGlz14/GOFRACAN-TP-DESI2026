package tuti.desi.entidades;

public enum EstadoContrato {
//enum en vez de class porque son estados especificos
// si pusiera class tendria que crear un metodo especifico para cada estado
	borrador,
	activo,
	finalizado,
	rescindido;

}